<Container>
        <form onSubmit={uploadFile} className="flex flex-col item-center p-8">
          <div className="flex flex-wrap -mx-3 mb-6 ">
            <div className="flex items-center space-x-4 flex-row w-full px-3  ">
              <FaUserAlt size={50} />
              <input
                type="text"
                className="inline-block align-middle bg-transparent w-full text-gray-700  rounded py-3 px-4 focus:outline-none "
                placeholder="Say something about it..."
              />
            </div>

            <div className="md:w-1/3 px-3 mb-6 md:mb-0">
              <label htmlFor="fileInput">
                <BsCardImage size={20} />
                {/* <img src="default_user.jpg" className="h-7 rounded-full" /> */}
              </label>
              <input
                id="fileInput"
                type="file"
                multiple
                accept="image/*"
                style={{ display: "none" }}
                onChange={(e) => selectedFile(e)}
              />
            </div>
            <div className="text-gray-700 text-center">
              <button
                className="bg-blue-600 hover:bg-blue-800 text-white font-bold px-4 py-2 rounded-full block m-auto"
                type="submit"
              >
                Post
              </button>
            </div>
          </div>
        </form>
      </Container>
      {images.length > 0 ? (
        <ImageContainer>
          <ImageList images={images} removeImage={removeImage} />
        </ImageContainer>
      ) : (
        <div>...</div>
      )}